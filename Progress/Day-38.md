📅 Day 38 – SQL Triggers
✅ Topics Covered
BEFORE UPDATE Trigger
AFTER INSERT Trigger
Preventing Invalid Data
Automatic Logging using Triggers
📝 Queries Practiced
Query 1: BEFORE UPDATE Trigger

Created a trigger to prevent negative salary values from being updated in the cleaned_salaries table.

Query 2: AFTER INSERT Trigger

Created an attendance log table and an AFTER INSERT trigger to automatically record every new attendance entry.

💡 What I Learned
How SQL Triggers automate database actions.
Difference between BEFORE and AFTER triggers.
Using NEW and OLD values inside triggers.
Maintaining data integrity with triggers.
Automatically logging database events.
